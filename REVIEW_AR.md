# مراجعة مستودع MoneyPrinterTurbo

**المستودع:** `semoozaher-wq/money-printer-turbo`  
**الفرع/النسخة المفحوصة:** `main`، commit `f152ee8`  
**تاريخ المراجعة:** 2026-09-13

## الخلاصة التنفيذية

المشروع غني وظيفيًا ويحتوي على قرارات جيدة في عدة أماكن، منها تثبيت إصدارات التبعيات في `pyproject.toml` و`uv.lock`، استخدام نماذج تحقق للـ CLI، وجود حماية لمسارات ملفات المهام في `app/asgi.py`، وإضافة إخفاء للأسرار عند تسجيل بعض أخطاء مزودي الخدمات. كما أن الشيفرة اجتازت فحص الصياغة (`compileall`) دون أخطاء.

مع ذلك، لا أوصي بتعريض النسخة الحالية مباشرة للإنترنت أو تشغيلها كخدمة عامة متعددة المستخدمين قبل معالجة عناصر **عالية الأولوية**: المصادقة الافتراضية، حدود الموارد والرفع، دورة حياة ملفات المهام، تشغيل Docker، والاختبارات الآلية. المشروع مناسب حاليًا للاستخدام المحلي أو خلف reverse proxy موثوق، مع ضبط يدوي للإعدادات.

## نتائج الفحص

| المجال | الحالة | الملاحظة |
|---|---|---|
| الصياغة والاستيراد | جيد | `python3 -m compileall -q app webui main.py cli.py` نجح. |
| الاختبارات | ضعيف | يوجد اختبار واحد ظاهر في `test/test_main.py`، و`pytest` غير مثبت في بيئة الفحص. لا توجد تغطية عملية كافية لمسارات API والفيديو والرفع. |
| التبعيات | متوسط/جيد | الإصدارات مثبتة، لكن توجد قائمتان للتبعيات (`pyproject.toml` و`requirements.txt`) يجب منعهما من الانحراف. |
| API والأمان | يحتاج ضبطًا | المصادقة اختيارية عندما لا يتم ضبط `app.api_key`؛ هذا مناسب للتطوير فقط وليس للنشر العام. |
| الملفات والتخزين | يحتاج ضبطًا | ملفات المهام والفيديوهات قد تكبر وتتراكم؛ يلزم retention وquota وتنظيف موثق ومجدول. |
| Docker/التشغيل | متوسط | لا تظهر healthchecks أو مستخدم non-root أو حدود موارد واضحة في Compose. |
| القابلية للتوسع | محدودة | إدارة المهام في الذاكرة، مع Redis اختياري/موجود ضمن التبعيات؛ إعادة تشغيل العملية قد تفقد حالة المهام المحلية. |
| المراقبة | محدودة | توجد سجلات جيدة نسبيًا، لكن لا يظهر نظام موحد للـ metrics وrequest IDs وhealth/readiness منفصلين. |

## ما يجب إصلاحه أولًا

### P0 — منع التشغيل العام بلا مصادقة

في `app/controllers/base.py` يتم السماح بالطلبات عندما تكون قيمة `config.app.api_key` فارغة. هذا يجعل جميع نقاط API وملفات `/tasks` متاحة لأي عميل يصل إلى المنفذ. وجود CORS لا يعوض المصادقة؛ CORS حماية للمتصفح وليست تحكمًا في الوصول.

**الإجراء المقترح:**

1. اجعل `api_key` إلزاميًا في وضع الإنتاج.
2. أوقف الإقلاع برسالة واضحة إذا كان `ENVIRONMENT=production` والمفتاح غير موجود.
3. استخدم مقارنة ثابتة الزمن (`secrets.compare_digest`).
4. لا تسمح بالمفتاح في query string؛ اجعله في `X-API-Key` أو `Authorization: Bearer` فقط.
5. أضف rate limiting، ويفضل وضعه في reverse proxy أيضًا.

مثال سياسة تشغيل:

```python
if settings.environment == "production" and not settings.api_key:
    raise RuntimeError("APP_API_KEY must be configured in production")
```

### P0 — فرض حدود على الرفع والموارد

المشروع يتعامل مع فيديوهات وصوت وعمليات FFmpeg وطلبات مزودين خارجيين. من دون quota واضحة يمكن لملف كبير أو عدد مهام متزامن أن يستهلك الذاكرة، المعالج، مساحة القرص أو رصيد API.

**الإصلاح المطلوب:**

- حد أقصى لحجم كل ملف، وعدد الملفات لكل طلب.
- السماح بقائمة امتدادات وMIME types مع فحص فعلي للنوع، لا الاكتفاء باسم الملف.
- حد أقصى لمدة الفيديو ودقة الإخراج.
- semaphore عام لعمليات التوليد الثقيلة.
- حد أقصى لطول طابور المهام مع رد `429` عند الامتلاء.
- timeout وإلغاء حقيقي للمهام العالقة.
- تسجيل استهلاك CPU/الذاكرة/القرص ومدة كل مرحلة.

### P0 — إدارة عمر ملفات المهام

الملفات تُكتب تحت `storage/tasks/<task-id>/`، وتوجد أدوات تنظيف للكاش، لكن يجب جعل سياسة الاحتفاظ بملفات المهام نفسها صريحة ومؤتمتة. فيديوهات الإنتاج قد تملأ القرص حتى لو كان الكاش ينظف بشكل صحيح.

**الإصلاح المطلوب:**

- متغيرات مثل `TASK_RETENTION_DAYS` و`MAX_STORAGE_BYTES`.
- job دوري يحذف المهام النهائية القديمة فقط، ولا يحذف المهام قيد التنفيذ.
- قفل/marker يثبت أن المهمة ما زالت قيد الكتابة.
- حذف ذري أو نقل إلى مجلد quarantine قبل الحذف النهائي.
- endpoint إداري محمي لعرض الاستخدام والتنظيف اليدوي.
- اختبار أن `task_id` لا يمكن أن يؤدي إلى traversal؛ التحقق موجود في بعض المسارات ويجب توحيده في كل موضع.

### P1 — فصل إدارة المهام عن ذاكرة العملية

إدارة المهام المحلية في `app/controllers/manager/memory_manager.py` مناسبة للتطوير، لكنها تفقد الطابور والحالة عند إعادة تشغيل العملية، ولا تناسب أكثر من worker أو أكثر من نسخة.

**التحديث المقترح:**

- اعتماد Redis كـ backend رسمي للـ queue والحالة، أو توثيق أن الوضع المحلي development-only.
- استخدام worker منفصل لـ FFmpeg والتوليد بدل تنفيذ الأعمال الثقيلة داخل عملية الويب.
- تخزين metadata المهمة في Redis/قاعدة بيانات، والملفات في object storage أو volume مخصص.
- جعل إعادة التشغيل idempotent: المهمة التي كانت `running` تتحول إلى `requeued` أو `failed` مع سبب واضح.

### P1 — تحسين Docker وCompose

في `docker-compose.yml` توجد خدمات webui وapi، لكن لا تظهر healthchecks أو حدود موارد أو مستخدم غير root. كما أن الـ volume الحالي يربط كامل المشروع داخل الحاوية (`./:/MoneyPrinterTurbo`)، وهذا مناسب للتطوير لكنه يخلط الشيفرة مع البيانات ويزيد سطح التعديل غير المقصود.

**الإصلاح المقترح:**

- استخدم multi-stage build أو صورة runtime أصغر.
- أنشئ مستخدمًا غير root وشغّل التطبيق به.
- اربط volumes منفصلة مثل `./storage:/MoneyPrinterTurbo/storage` و`./config.toml:/MoneyPrinterTurbo/config.toml:ro`.
- أضف `healthcheck` للـ API وWebUI.
- أضف `mem_limit` و`cpus` و`pids_limit` حسب بيئة التشغيل.
- ثبّت مسار FFmpeg صراحة واختبره عند الإقلاع.
- لا تستخدم `restart: always` بلا healthcheck وسياسة فشل واضحة.
- افصل compose development عن production.

مثال healthcheck للـ API:

```yaml
healthcheck:
  test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8080/ping"]
  interval: 30s
  timeout: 5s
  retries: 3
  start_period: 20s
```

### P1 — توحيد التبعيات وCI

المشروع يعلن أن المصدر الأساسي للتبعيات هو `pyproject.toml` و`uv.lock`، لكنه يحتفظ أيضًا بـ `requirements.txt`. هذا يفتح احتمال اختلاف بيئة Docker أو التثبيت التقليدي عن بيئة `uv`.

**الإصلاح المقترح:**

- اجعل Docker يستخدم `uv sync --frozen` من `uv.lock`، أو ولّد `requirements.txt` آليًا ولا تعدله يدويًا.
- أضف CI واضحًا ينفذ: `ruff check`, `ruff format --check`, `pytest`, `coverage`, وفحص build للصورة.
- أضف فحصًا للتبعيات (`pip-audit` أو `uv audit` إن كان متاحًا في بيئة CI).
- شغّل CI على Python 3.11 و3.12 و3.13، وعلى Linux على الأقل.
- ثبّت نسخة FFmpeg في CI أو نفذ smoke test لاكتشاف اختلافاتها.

### P1 — بناء مجموعة اختبارات حقيقية

الاختبار الظاهر حاليًا يتحقق من استدعاء Uvicorn فقط. يلزم تغطية الطبقات الحرجة قبل أي refactor كبير.

**الاختبارات المقترحة:**

1. `TestClient` لمسارات ping وvideo وllm.
2. رفض API request عند غياب/خطأ المفتاح.
3. قبول المفتاح الصحيح وعدم تسريبه في السجلات.
4. رفض Origin غير المسموح ونجاح preflight المسموح.
5. اختبارات path traversal في `task_id` واسم الملف.
6. رفع ملف كبير/امتداد خاطئ/MIME خاطئ.
7. انتقالات حالة المهمة: queued → processing → completed/failed.
8. إعادة تشغيل worker واستعادة المهام عند استخدام Redis.
9. cleanup لا يحذف الملفات الحديثة أو المهام قيد التنفيذ.
10. اختبارات mock لمزودي LLM/TTS/video مع timeout و429 و5xx.
11. اختبار تكامل صغير لـ FFmpeg بملف صوت/فيديو قصير.

## تحسينات مهمة لاحقة

### إعدادات وأسرار

أسرار مزودي الخدمات تُحفظ ضمن إعدادات التطبيق وواجهة Streamlit. يجب توثيق أن `config.toml` لا يدخل Git، إضافة `.gitignore` وفحص secret scanning في CI، وعدم تضمين قيمة المفتاح في snapshots أو أخطاء الاستثناءات. الأفضل دعم متغيرات البيئة كخيار أول في الإنتاج مع أولوية واضحة:

`ENV` ثم secret manager ثم `config.toml` للتطوير فقط.

ينبغي أيضًا تقليل عدد الأماكن التي تقرأ المفاتيح مباشرة من `config`، وتوفير طبقة واحدة مثل `CredentialProvider` مع redaction مركزي.

### الاتصالات الخارجية

يوجد عدد كبير من مزودي LLM/TTS/material/video. أوصي بتوحيد عميل HTTP داخل المشروع لاستخدام:

- timeout للاتصال والقراءة.
- retry محدود مع exponential backoff وبدون إعادة محاولة أخطاء 4xx غير القابلة للإصلاح.
- احترام `Retry-After`.
- circuit breaker عند تكرار فشل مزود معين.
- إخفاء Authorization headers من السجلات.
- تحديد حجم الاستجابة قبل تنزيلها إلى الذاكرة.

### المراقبة والتشخيص

أضف request ID ينتقل من API إلى task وإلى كل سجل، مع structured logging بصيغة JSON في الإنتاج. أضف metrics مثل عدد المهام حسب الحالة، زمن كل مرحلة، فشل كل مزود، حجم الطابور، وحجم التخزين. يجب أن يكون `/health/live` خفيفًا، و`/health/ready` يتحقق من Redis/FFmpeg/مساحة القرص دون تنفيذ عملية توليد.

### هندسة الواجهة

`webui/Main.py` ملف كبير جدًا ويجمع العرض، قراءة الإعدادات، جدولة المهام، ومنطق مزودي الخدمات. هذا يرفع تكلفة التعديل والاختبار.

التحديث التدريجي المناسب:

- استخراج `config_ui.py` و`task_ui.py` و`provider_ui.py`.
- نقل نماذج البيانات والتحقق إلى `app/schemas`.
- إبقاء Streamlit طبقة عرض فقط.
- إضافة اختبارات للوحدات المستخرجة قبل حذف الكود القديم.

### الوثائق

أضف وثيقة تشغيل إنتاجي بالعربية أو الإنجليزية تتضمن: reverse proxy، TLS، المصادقة، إعداد Redis، volumes، backup، cleanup، حدود الموارد، وتدوير المفاتيح. يجب فصل وسم "للاستخدام المحلي" عن تعليمات النشر العام، مع تحذير صريح من فتح Streamlit/API مباشرة على الإنترنت.

## خطة تنفيذ مقترحة

| المرحلة | الأعمال | معيار الانتهاء |
|---|---|---|
| 1 | إلزام المصادقة في production، quotas، file validation، retention | لا يمكن الوصول المجهول، والطلبات الكبيرة تُرفض بأخطاء واضحة، والتنظيف قابل للقياس |
| 2 | Redis queue/state، worker منفصل، recovery بعد restart | لا تضيع حالة المهمة بعد إعادة التشغيل، ولا تنفذ الأعمال الثقيلة داخل web worker |
| 3 | CI واختبارات API/security/integration | CI أخضر على إصدارات Python المستهدفة وتغطية للطبقات الحرجة |
| 4 | Docker production profile، non-root، healthchecks، volumes منفصلة | image قابلة للتشغيل بأقل صلاحيات وبفحوص جاهزية |
| 5 | metrics وstructured logs وإعادة تنظيم WebUI | تشخيص فشل مزود أو مهمة من request ID واحد، وتقليل حجم الملفات المركزية |

## أمثلة تحديثات ذات أولوية

### تحقق موحد من الملفات

```python
ALLOWED_UPLOADS = {
    "video/mp4": ".mp4",
    "video/webm": ".webm",
    "audio/mpeg": ".mp3",
    "audio/wav": ".wav",
}
MAX_UPLOAD_BYTES = 500 * 1024 * 1024

async def validate_upload(upload: UploadFile) -> None:
    if upload.content_type not in ALLOWED_UPLOADS:
        raise HTTPException(415, "unsupported media type")
    # يجب تطبيق العد أثناء القراءة، لا الاعتماد على Content-Length فقط.
```

### سياسة بيئات واضحة

```toml
# config.toml
environment = "production"
api_key = ""                 # يفضل ENV/secret manager في الإنتاج
max_concurrent_tasks = 1
max_upload_bytes = 524288000
max_task_storage_bytes = 10737418240
task_retention_days = 7
```

## ما تم التحقق منه فعليًا

- تم استنساخ المستودع وفحص commit `f152ee8`.
- اجتازت ملفات Python فحص `compileall` دون أخطاء.
- لم يكن `pytest` متاحًا في بيئة الفحص، لذلك لم يتم ادعاء نجاح الاختبارات.
- ظهر اختبار واحد في المسار `test/test_main.py`، وهو اختبار نقطة تشغيل Uvicorn.
- تمت مراجعة ملفات الإعداد، Docker/Compose، API، إدارة المهام، التبعيات، والبحث عن أنماط الأسرار والعمليات الخارجية.

## الحكم النهائي

**للاستخدام المحلي:** جيد بعد ضبط المفاتيح وعدم فتح المنافذ للعامة.  
**لبيئة فريق داخل شبكة موثوقة:** ممكن مع reverse proxy ومصادقة وlimits ونسخ احتياطي.  
**لخدمة عامة/مدفوعة متعددة المستخدمين:** غير موصى به قبل تنفيذ بنود P0 وP1، خصوصًا إلزام المصادقة، quotas، إدارة التخزين، queue خارجية، واختبارات الأمان والتكامل.

أكبر قيمة عملية في التحديث ليست إضافة مزودي AI جدد، بل جعل التوليد **محدود الموارد، قابلًا للاسترداد بعد الأعطال، قابلًا للمراقبة، وآمنًا عند التعرض للشبكة**.
